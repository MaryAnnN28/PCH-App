import React, { useState } from 'react';

const Listings = () => {
    const [listings, setListings] = useState([]);

    return (
        <div>
            <h1>Listings Page</h1>
        </div>
    );
};

export default Listings;
