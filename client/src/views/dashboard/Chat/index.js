import React, { useState } from 'react';

const Chat = () => {
    const [team, setTeam] = useState([]);

    return (
        <div>
            <h1>Chat Page</h1>
        </div>
    );
};

export default Chat;
