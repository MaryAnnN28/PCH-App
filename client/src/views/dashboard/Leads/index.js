import React, { useState } from 'react';

const Leads = () => {
    const [leads, setLeads] = useState([]);
    return (
        <div>
            <h1>Leads Page</h1>
        </div>
    );
};

export default Leads;
